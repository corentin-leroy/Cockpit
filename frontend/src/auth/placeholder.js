// Placeholder — dossier `auth/`.
// Accueillera la gestion du token JWT (stockage, lecture) et le contexte
// d'authentification React (AuthContext, hook useAuth). Créés à l'étape suivante.
export {}
