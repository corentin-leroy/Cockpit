# Adresse du backend FastAPI appelé par le front en développement.
# Copier ce fichier en .env.local pour surcharger sans committer la valeur.
# Le préfixe VITE_ est obligatoire pour que Vite expose la variable au client.
VITE_API_BASE_URL=http://127.0.0.1:8000
